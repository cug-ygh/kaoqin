<text class="title">查询结果</text>
<view class="container">
    <view class="table">
        <view class="tr">
            <view class="th">姓名</view>
            <!-- <view class="th">学号</view> -->
            <!-- <view class="th">考勤号</view> -->
            <view class="th">迟到</view>
            <view class="th">早退</view>
            <view class="th">旷到</view>
            <!-- <view class="th">班号</view> -->
        </view>
        <view class="tr" wx:for="{{list}}" wx:for-item="item" wx:key="{{list}}">
            <view class="td">{{item.name}}</view>
            <!-- <view class="td">{{item.stu_num}}</view> -->
            <!-- <view class="td">{{item.kq_num}}</view> -->
            <view class="td">{{item.late}}</view>
            <view class="td">{{item.leave}}</view>
            <view class="td">{{item.noab}}</view>
            <!-- <view class="td">{{item.class_num}}</view> -->
        </view>
    </view>
</view>

